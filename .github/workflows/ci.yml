name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: cboone/gh-actions/.github/workflows/go-ci.yml@main
    with:
      run-lint: false
      run-scrut: true
      scrut-build-cmd: "go build ."
